{% extends 'base.html.twig' %}

{% block title %}Space Invaders{% endblock %}

{% block stylesheets %}
    <link href="/css/spaceinvaders.css" rel="stylesheet" type="text/css">
    <link href='https://fonts.googleapis.com/css?family=Play:400,700' rel='stylesheet' type='text/css'>
{% endblock %}

{% block body %}
    <div class="grille">
        <div class="instruction">
            <p>
                <strong class="important">Comment jouer :</strong> Appuyez sur entrer pour commencer <br/><br/>
                Se déplacer : <strong>Q</strong> et <strong>D</strong> <br/>
                Tirer : <strong>Entrée</strong>
            </p>
        </div>
        <div class="tableau-score scale-up-hor-right">
            {% if app.user %}
                <p class="ScorePerso">Votre meilleur score est de <b>{{ best_score_perso }}</b></p>
            {% endif %}
            <h3 style="text-align:center;">Top 10 des joueurs</h3>
            <table id="classement" class="table table-striped table-hover table-bordered align-middle text-center" style="margin:auto;">
                <thead class="table-dark">
                    <tr>
                        <th>Place</th>
                        <th>Joueurs</th>
                        <th>Score</th>
                    </tr>
                </thead>
                <tbody>
                {% set compteur = 1 %}
                {% set compareScore = 0 %}
                {% set place = 1 %}
                {% for user in orderedUsers %}
                    {% if compteur <= 10 %}
                        {% if compareScore == user.scoreSpaceInvaders %}
                            <tr id="place_{{ place }}">
                                <td>{{ place }}</td>
                        {% else %} 
                            <tr id="place_{{ compteur }}">
                                <td>{{ compteur }}</td>
                            {% set place = compteur %}
                        {% endif %} 
                                <td>{{ user.firstname }} {{ user.name }}</td>
                                <td>{{ user.scoreSpaceInvaders }}</td>
                            </tr>
                        {% set compteur = compteur + 1 %}
                        {% set compareScore = user.scoreSpaceInvaders %}
                    {% endif %}  
                {% endfor %}
            </table>
        </div>
        <div class="game">
            <canvas id="game-canvas" width="640" height="640"></canvas>
            {% if app.user %}
                <p class="ScorePerso"> Tu es connecté(e) ! Ton meilleur score sera enregistré 😊</p>
            {% else %}
                <p class="ScorePerso"> Connectez-vous pour connaître votre meilleur score et pour l'enregistrer 😁<p>
            {% endif %}
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    <script type="text/javascript" src="/js/games/spaceinvaders.js"></script>
    <script type='text/javascript' src='/js/confetti.browser.min.js'></script>
  
    <script type="text/javascript">
        $(document).ready(function(){
            var theme = sessionStorage.getItem('theme');
            if (theme == 'dark') {
                $("#dark-game").addClass('text-dark');
                $("#classement").addClass('table-dark');
            } else {
                $("#dark-game").removeClass('text-dark');
                $("#classement").removeClass('table-dark');
            }
        });

        setInterval(displayConfetti, 1000);

        function displayConfetti() {
            if (sessionStorage.getItem("game")) {
                var duration = 5 * 1000;
                var animationEnd = Date.now() + duration;
                var defaults = { startVelocity: 30, spread: 360, ticks: 60, zIndex: 0 };

                function randomInRange(min, max) {
                    return Math.random() * (max - min) + min;
                }

                var interval = setInterval(function() {
                    var timeLeft = animationEnd - Date.now();

                    if (timeLeft <= 0) {
                        return clearInterval(interval);
                    }

                    var particleCount = 50 * (timeLeft / duration);
                    // since particles fall down, start a bit higher than random
                    confetti(Object.assign({}, defaults, { particleCount, origin: { x: randomInRange(0.1, 0.3), y: Math.random() - 0.2 } }));
                    confetti(Object.assign({}, defaults, { particleCount, origin: { x: randomInRange(0.7, 0.9), y: Math.random() - 0.2 } }));
                }, 250);
                sessionStorage.removeItem('game');
            }
        }
    </script>
{% endblock %}
